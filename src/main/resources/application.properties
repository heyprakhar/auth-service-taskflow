# ─────────────────────────────────────────────
# Application Identity
# ─────────────────────────────────────────────
# Identifies this service in Eureka, logs, and distributed traces.
spring.application.name=AUTH-SERVICE

# eureka discovery client configuration - 
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

logging.level.root=INFO


# server
server.port=8082

# database
spring.datasource.url=jdbc:mysql://localhost:3306/taskflow_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Harshiv#1134

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=c2VjdXJlS2V5VGhhdFNob3VsZEFiZVNldmVyQnl0ZXM= 
jwt.access.expiration=3600000
jwt.refresh.expiration=86400000

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%X{traceId}] [%thread] %-5level %logger{36} - %msg%n